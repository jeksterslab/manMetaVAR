% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manMetaVAR-methods-data.R,
%   R/manMetaVAR-methods-dt-var.R, R/manMetaVAR-methods-meta-var.R,
%   R/manMetaVAR-methods-ml-var.R, R/manMetaVAR-methods-mplus.R
\name{summary.manmetavar.data}
\alias{summary.manmetavar.data}
\alias{summary.manmetavar.dtvar}
\alias{summary.manmetavar.metavar}
\alias{summary.manmetavar.mlvar}
\alias{summary.manmetavar.mplus}
\title{Summary Method for an Object of Class \code{manmetavar.data}}
\usage{
\method{summary}{manmetavar.data}(object, ...)

\method{summary}{manmetavar.dtvar}(
  object,
  means = FALSE,
  alpha = TRUE,
  beta = TRUE,
  nu = TRUE,
  psi = TRUE,
  theta = TRUE,
  converged = TRUE,
  grad_tol = 0.01,
  hess_tol = 1e-08,
  vanishing_theta = TRUE,
  theta_tol = 0.001,
  digits = 4,
  ...
)

\method{summary}{manmetavar.metavar}(object, alpha = 0.05, digits = 4, ...)

\method{summary}{manmetavar.mlvar}(
  object,
  show = c("fit", "temporal", "contemporaneous", "between"),
  digits = 4,
  ...
)

\method{summary}{manmetavar.mplus}(object, alpha = 0.05, median = TRUE, digits = 4, ...)
}
\arguments{
\item{object}{Object of class \code{manmetavar.mplus}.}

\item{...}{additional arguments.}

\item{means}{Logical.
If \code{means = TRUE}, return means.
Otherwise, the function returns raw estimates.}

\item{alpha}{Numeric vector.
Significance level \eqn{\alpha}.}

\item{beta}{Logical.
If \code{beta = TRUE},
include estimates of the \code{beta} matrix, if available.
If \code{beta = FALSE},
exclude estimates of the \code{beta} matrix.}

\item{nu}{Logical.
If \code{nu = TRUE},
include estimates of the \code{nu} vector, if available.
If \code{nu = FALSE},
exclude estimates of the \code{nu} vector.}

\item{psi}{Logical.
If \code{psi = TRUE},
include estimates of the \code{psi} matrix, if available.
If \code{psi = FALSE},
exclude estimates of the \code{psi} matrix.}

\item{theta}{Logical.
If \code{theta = TRUE},
include estimates of the \code{theta} matrix, if available.
If \code{theta = FALSE},
exclude estimates of the \code{theta} matrix.}

\item{converged}{Logical.
Only include converged cases.}

\item{grad_tol}{Numeric scalar.
Tolerance for the maximum absolute gradient
if \code{converged = TRUE}.}

\item{hess_tol}{Numeric scalar.
Tolerance for Hessian eigenvalues;
eigenvalues must be strictly greater than this value
if \code{converged = TRUE}.}

\item{vanishing_theta}{Logical.
Test for measurement error variance going to zero
if \code{converged = TRUE}.}

\item{theta_tol}{Numeric.
Tolerance for vanishing theta test
if \code{converged} and \code{theta_tol} are \code{TRUE}.}

\item{digits}{Integer indicating the number of decimal places to display.}

\item{show}{Tables to show.}

\item{median}{Logical.
If \code{median = TRUE}, return median of the posterior.
If \code{median = FALSE}, return mean of the posterior.}
}
\value{
Returns a matrix of
estimates,
standard errors,
test statistics,
degrees of freedom,
p-values,
and
confidence intervals.

Returns a matrix of
estimates,
standard errors,
test statistics,
degrees of freedom,
p-values,
and
confidence intervals.

Returns a matrix of estimates, standard errors,
number of posterior samples, and confidence intervals.
}
\description{
Summary Method for an Object of Class \code{manmetavar.data}

Summary Method (FitDTVAR)

Summary Method (FitMetaVAR)

Summary Method (FitMLVAR)

Summary Method (FitMplus)
}
\author{
Ivan Jacob Agaloos Pesigan
}
\keyword{methods}

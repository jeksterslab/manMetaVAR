% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manMetaVAR-fig-mplus-diagnostics-overview.R
\name{FigMplusDiagnosticsOverview}
\alias{FigMplusDiagnosticsOverview}
\title{Compact Bayesian Diagnostic Overview Across Simulation Conditions}
\usage{
FigMplusDiagnosticsOverview(
  diagnostics,
  comparison = "difference",
  diagnostic = "Any diagnostic",
  unit = "replication",
  taskid = NULL,
  target = NULL,
  rhat_threshold = 1.01,
  ess_threshold = 400,
  mcse_threshold = 0.05,
  relative_mcse = TRUE,
  grey_scale = FALSE,
  values = FALSE,
  label_threshold = 0.05
)
}
\arguments{
\item{diagnostics}{An all-task diagnostics object containing a \code{parameters}
data frame, or the parameter-level data frame itself.}

\item{comparison}{Character string. Use \code{"levels"} to show diagnostic
failure rates under each prior specification or \code{"difference"} to show
paired user-prior minus default-prior differences.}

\item{diagnostic}{Diagnostic failure panels to display. Available values are
\code{"Any diagnostic"}, \code{"R-hat"}, \code{"Bulk ESS"}, \code{"Tail ESS"}, and
\code{"Relative MCSE"} when \code{relative_mcse = TRUE}.}

\item{unit}{Character string controlling the unit summarized within each
design cell. \code{"replication"} classifies a replication as failed when any
parameter in the selected parameter block fails. \code{"parameter"} averages
failure indicators across parameter-replication pairs.}

\item{taskid}{Optional positive integer task IDs to include.}

\item{target}{Optional parameter blocks to include. Available values are
\code{"Innovation"}, \code{"FE"}, and \code{"RE"}.}

\item{rhat_threshold}{Numeric R-hat threshold.}

\item{ess_threshold}{Numeric bulk and tail ESS threshold.}

\item{mcse_threshold}{Numeric MCSE threshold.}

\item{relative_mcse}{Logical. If \code{TRUE}, Monte Carlo standard errors are
divided by the posterior standard deviation before classifying failures.}

\item{grey_scale}{Logical. If \code{TRUE}, use grey-scale fills.}

\item{values}{Logical. If \code{TRUE}, print values inside sufficiently large
cells.}

\item{label_threshold}{Numeric value between zero and one. Labels with an
absolute value smaller than this threshold are omitted. For level plots,
the threshold applies directly to the failure rate.}
}
\value{
A \code{ggplot} object. Its \code{data} element contains the block-level
summaries used in the figure.
}
\description{
Summarize Mplus Bayesian diagnostic failures across all simulation task IDs
at the Innovation, fixed-effect, and random-effect block level. This compact
overview is intended for manuscript figures and reviewer-oriented prior
sensitivity analyses. Use \code{\link[=FigMplusDiagnosticsHeatmap]{FigMplusDiagnosticsHeatmap()}} for parameter-level
drill-down figures.
}
\details{
The default \code{unit = "replication"} answers a reviewer-friendly question:
what proportion of simulation replications had at least one diagnostic
problem within each parameter block? For \code{comparison = "difference"}, the
function pairs prior specifications within task ID and replication before
calculating user-prior minus default-prior differences. Negative values
indicate fewer failures under user-specified priors.
}
\examples{
\dontrun{
data(diagnostics, package = "manMetaVAR")

FigMplusDiagnosticsOverview(
  diagnostics = diagnostics,
  comparison = "levels"
)

FigMplusDiagnosticsOverview(
  diagnostics = diagnostics,
  comparison = "difference",
  values = TRUE
)
}

}
\seealso{
Other Figure Functions:
\code{\link[=FigMetricHeatmap]{FigMetricHeatmap()}},
\code{\link[=FigMplusDiagnosticsHeatmap]{FigMplusDiagnosticsHeatmap()}},
\code{\link[=FigOverview]{FigOverview()}}
}
\author{
Ivan Jacob Agaloos Pesigan
}
\concept{Figure Functions}
\keyword{diagnostics}
\keyword{figure}
\keyword{manMetaVAR}
\keyword{sensitivity}
\keyword{simulation}

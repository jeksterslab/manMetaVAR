% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manMetaVAR-tab-mplus-diagnostics-sensitivity.R
\name{TabMplusDiagSensitivity}
\alias{TabMplusDiagSensitivity}
\title{Bayesian Diagnostic Failure-Rate Sensitivity Table}
\usage{
TabMplusDiagSensitivity(
  diagnostics,
  comparison = "difference",
  diagnostic = "Any diagnostic",
  unit = "replication",
  taskid = NULL,
  target = NULL,
  rhat_threshold = 1.01,
  ess_threshold = 400,
  mcse_threshold = 0.05,
  relative_mcse = TRUE,
  output = "data.frame",
  scale = "percent",
  digits = 0L
)
}
\arguments{
\item{diagnostics}{An all-task diagnostics object containing a \code{parameters}
data frame, or the parameter-level data frame itself.}

\item{comparison}{Character string. \code{"difference"} returns paired
user-prior minus default-prior failure-rate differences. \code{"levels"}
returns the two prior-specific failure rates. \code{"all"} returns the default
rate, user-prior rate, and paired difference.}

\item{diagnostic}{One or more diagnostic panels. Available values are
\code{"Any diagnostic"}, \code{"R-hat"}, \code{"Bulk ESS"}, \code{"Tail ESS"}, and
\code{"Relative MCSE"} when \code{relative_mcse = TRUE}.}

\item{unit}{Character string controlling the unit classified as failed.
\code{"replication"} classifies a replication as failed when any parameter in
the selected block fails. \code{"parameter"} averages failures over
parameter-replication pairs.}

\item{taskid}{Optional positive integer task IDs to include.}

\item{target}{Optional parameter blocks to include. Available values are
\code{"Innovation"}, \code{"FE"}, and \code{"RE"}.}

\item{rhat_threshold}{Numeric R-hat threshold.}

\item{ess_threshold}{Numeric bulk and tail effective sample size threshold.}

\item{mcse_threshold}{Numeric Monte Carlo standard error threshold.}

\item{relative_mcse}{Logical. If \code{TRUE}, Monte Carlo standard errors are
divided by the posterior standard deviation before classifying failures.}

\item{output}{Character string. \code{"data.frame"} returns a wide data frame
with simulation-design columns, \code{"matrix"} returns only the numeric
case-by-summary matrix, and \code{"long"} returns one row per task, block,
diagnostic, and prior comparison.}

\item{scale}{Character string. \code{"proportion"} returns values on the 0-to-1
scale. \code{"percent"} multiplies prior-specific failure rates by 100 and
expresses differences in percentage points.}

\item{digits}{Nonnegative integer used to round the returned values.}
}
\value{
A data frame or numeric matrix. For \code{output = "matrix"}, simulation
design information is stored in the \code{"case_data"} attribute.
}
\description{
Create a simulation-case-level table of Bayesian Mplus diagnostic failure
rates. Unlike the overview figure, the function retains one row for every
simulation task ID and does not average across simulation cases.
}
\details{
Failure rates are computed separately within each task ID. For
\code{comparison = "difference"}, replications are paired before calculating
the user-prior minus default-prior difference. Negative differences favor
the user-specified priors.

The default \code{unit = "replication"} answers the question: within a given
simulation case, what percentage of replications had at least one failed
parameter in the selected parameter block?
}
\examples{
\dontrun{
data(diagnostics, package = "manMetaVAR")

# Compact sensitivity matrix: one row per simulation case
TabMplusDiagSensitivity(
  diagnostics = diagnostics,
  comparison = "difference",
  output = "matrix"
)

# Default, user-prior, and difference columns
TabMplusDiagSensitivity(
  diagnostics = diagnostics,
  comparison = "all",
  output = "data.frame"
)

# Diagnostic-specific table
TabMplusDiagSensitivity(
  diagnostics = diagnostics,
  comparison = "difference",
  diagnostic = c("R-hat", "Bulk ESS", "Tail ESS", "Relative MCSE")
)
}

}
\author{
Ivan Jacob Agaloos Pesigan
}
\keyword{diagnostics}
\keyword{manMetaVAR}
\keyword{sensitivity}
\keyword{simulation}
\keyword{table}

---
title: "Single Replication from the Simulation Study"
author: "Ivan Jacob Agaloos Pesigan"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Single Replication from the Simulation Study}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r}
#| include = FALSE
knitr::opts_chunk$set(
  collapse = TRUE,
  fig.path = "fig-vignettes-sim-rep-",
  fig.cap = "",
  fig.width = 11,
  fig.height = 8,
  fig.retina = 2,
  dpi = 300,
  comment = "#>",
  dev = "ragg_png"
)
```

```{r}
#| message = FALSE
library(OpenMx)
library(fitVARMxID)
library(metaDyn)
library(manMetaVAR)
```

```{r}
#| include = FALSE
root <- rprojroot::is_rstudio_project
data_raw <- root$find_file(
  ".setup",
  "data-raw"
)
if (!dir.exists(data_raw)) {
  dir.create(data_raw, recursive = TRUE)
}
```

```{r}
#| include = FALSE
fn_data <- root$find_file(
  ".setup",
  "data-raw",
  "sim-rep-data.Rds"
)
if (!file.exists(fn_data)) {
  taskid <- 10
  seed <- 42
  set.seed(seed)
  data <- GenData(
    taskid = taskid,
    seed = seed
  )
  con <- xzfile(
    description = fn_data,
    compression = 9L
  )
  saveRDS(
    object = data,
    file = con
  )
  close(con)
}
data <- readRDS(fn_data)
```

```{r}
#| include = FALSE
fn_dtvar <- root$find_file(
  ".setup",
  "data-raw",
  "sim-rep-dtvar.Rds"
)
if (!file.exists(fn_dtvar)) {
  dtvar <- FitDTVAR(
    data = data,
    seed = seed
  )
  con <- xzfile(
    description = fn_dtvar,
    compression = 9L
  )
  saveRDS(
    object = dtvar,
    file = con
  )
  close(con)
}
dtvar <- readRDS(fn_dtvar)
```

```{r}
#| include = FALSE
fn_metavar <- root$find_file(
  ".setup",
  "data-raw",
  "sim-rep-metavar.Rds"
)
if (!file.exists(fn_metavar)) {
  metavar <- FitMetaVAR(
    fit = dtvar,
    seed = seed
  )
  con <- xzfile(
    description = fn_metavar,
    compression = 9L
  )
  saveRDS(
    object = metavar,
    file = con
  )
  close(con)
}
metavar <- readRDS(fn_metavar)
```

```{r}
#| include = FALSE
fn_naive <- root$find_file(
  ".setup",
  "data-raw",
  "sim-rep-naive.Rds"
)
if (!file.exists(fn_naive)) {
  naive <- FitNaive(
    fit = dtvar
  )
  con <- xzfile(
    description = fn_naive,
    compression = 9L
  )
  saveRDS(
    object = naive,
    file = con
  )
  close(con)
}
naive <- readRDS(fn_naive)
```

```{r}
#| include = FALSE
fn_mplus_default <- root$find_file(
  ".setup",
  "data-raw",
  "sim-rep-mplus-default.Rds"
)
if (!file.exists(fn_mplus_default)) {
  mplus_default <- FitMplus(
    data = data,
    default_priors = TRUE,
    seed = seed
  )
  con <- xzfile(
    description = fn_mplus_default,
    compression = 9L
  )
  saveRDS(
    object = mplus_default,
    file = con
  )
  close(con)
}
mplus_default <- readRDS(fn_mplus_default)
```

```{r}
#| include = FALSE
fn_mplus_prior <- root$find_file(
  ".setup",
  "data-raw",
  "sim-rep-mplus-prior.Rds"
)
if (!file.exists(fn_mplus_prior)) {
  mplus_prior <- FitMplus(
    data = data,
    default_priors = FALSE,
    seed = seed
  )
  con <- xzfile(
    description = fn_mplus_prior,
    compression = 9L
  )
  saveRDS(
    object = mplus_prior,
    file = con
  )
  close(con)
}
mplus_prior <- readRDS(fn_mplus_prior)
```

## Data Generation

```{r}
#| eval = FALSE
taskid <- 10
seed <- 42
set.seed(seed)
data <- GenData(
  taskid = taskid,
  seed = seed
)
```

```{r}
plot(data)
summary(data)
```

## MetaVAR

```{r}
#| eval = FALSE
dtvar <- FitDTVAR(
  data = data,
  seed = seed
)
```

```{r}
summary(dtvar, means = TRUE)
```

```{r}
#| eval = FALSE
metavar <- FitMetaVAR(
  fit = dtvar,
  seed = seed
)
```

```{r}
summary(metavar)
```

## Mplus (Default Priors)

```{r}
#| eval = FALSE
  mplus_default <- FitMplus(
    data = data,
    default_priors = TRUE,
    seed = seed
  )
```

```{r}
summary(mplus_default)
```

```{r}
coef(mplus_default)
```

```{r}
vcov(mplus_default)
```

### Posterior Distributions

```{r}
plot(mplus_default, what = "posterior")
```

### Trace Plots

```{r}
plot(mplus_default, what = "trace")
```

### Diagnostics

```{r}
FitMplusDiagnostics(mplus_default)
```

### Mplus Output

```{r}
#| echo = FALSE,
#| comment = NA,
#| size = "normalsize"
writeLines(
  text = mplus_default$output$output,
  con = "mplus_default.inp"
)
cat(
  paste(
    readLines(
      "mplus_default.inp"
    ),
    collapse = "\n"
  )
)
```

## Mplus (User Defined Priors)

```{r}
#| eval = FALSE
  mplus_prior <- FitMplus(
    data = data,
    default_priors = TRUE,
    seed = seed
  )
```

```{r}
summary(mplus_prior)
```

```{r}
coef(mplus_prior)
```

```{r}
vcov(mplus_prior)
```

### Posterior Distributions

```{r}
plot(mplus_prior, what = "posterior")
```

### Trace Plots

```{r}
plot(mplus_prior, what = "trace")
```

### Diagnostics

```{r}
FitMplusDiagnostics(mplus_prior)
```

### Mplus Output

```{r}
#| echo = FALSE,
#| comment = NA,
#| size = "normalsize"
writeLines(
  text = mplus_prior$output$output,
  con = "mplus_prior.inp"
)
cat(
  paste(
    readLines(
      "mplus_prior.inp"
    ),
    collapse = "\n"
  )
)
```

## Uncorrected Two-Stage Benchmark

```{r}
#| eval = FALSE
naive <- FitNaive(
  fit = dtvar
)
```

```{r}
summary(naive)
```

```{r, rm}
#| include = FALSE
rm(list = ls())
```

---
title: "Single Replication from the Simulation Study"
author: "Ivan Jacob Agaloos Pesigan"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Single Replication from the Simulation Study}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r}
#| include = FALSE
knitr::opts_chunk$set(
  collapse = TRUE,
  fig.path = "fig-vignettes-sim-rep-",
  fig.cap = "",
  fig.width = 11,
  fig.height = 8,
  fig.retina = 2,
  dpi = 300,
  comment = "#>",
  dev = "ragg_png"
)
```

```{r}
#| include = FALSE
root <- rprojroot::is_rstudio_project
```

```{r}
#| message = FALSE
library(manMetaVAR)
```

## Data Generation

```{r}
#| include = FALSE
taskid <- 40
seed <- 42
set.seed(seed)
data <- GenData(taskid = taskid)
dtvar <- FitDTVAR(data, ncores = parallel::detectCores())
metavar <- FitMetaVAR(dtvar, ncores = parallel::detectCores())
mlvar <- FitMLVAR(data)
jags <- FitJAGS(data)
```

```{r}
#| eval = FALSE
taskid <- 40
seed <- 42
set.seed(seed)
data <- GenData(taskid = taskid)
```

```{r}
plot(data)
summary(data)
```

## MetaVAR

```{r}
#| eval = FALSE
dtvar <- FitDTVAR(data, ncores = parallel::detectCores())
```

```{r}
summary(dtvar, means = TRUE)
```

```{r}
#| eval = FALSE
metavar <- FitMetaVAR(dtvar, ncores = parallel::detectCores())
```

```{r}
summary(metavar)
```

## MLVAR

```{r}
#| eval = FALSE
mlvar <- FitMLVAR(data)
```

```{r}
summary(mlvar)
```

## JAGS

```{r}
#| eval = FALSE
jags <- FitJAGS(data)
```

```{r}
summary(jags)
```

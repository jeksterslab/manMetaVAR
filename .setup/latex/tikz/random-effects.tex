\documentclass[tikz, border=1mm]{standalone}

% convert -density 300 ml.pdf -quality 100 -background white -alpha remove -alpha off ml.png

\begin{document}

\usetikzlibrary{
	er,
	arrows,
	positioning
}

\begin{tikzpicture}[
		auto,
		node distance = 20mm,
		latent/.style = {
			ellipse,
			draw,
			thick,
			inner sep = 0pt,
			minimum size = 15mm,
			align = center
		},
		manifest/.style = {
			rectangle,
			draw,
			thick,
			inner sep = 0pt,
			minimum width = 15mm,
			minimum height = 10mm
		},
		inv/.style = {
			rectangle,
			draw=none,
			fill=none,
			inner sep = 0pt,
			minimum width = 15mm,
			minimum height = 10mm
		},
		error/.style = {
			ellipse,
			draw,
			thick,
			inner sep = 0pt,
			minimum size = 7mm,
			align = center
		},
		mean/.style={
			regular polygon,
			regular polygon sides = 3,
			draw,
			thick,
			inner sep = 0pt,
			minimum width = 7mm,
			minimum height = 7mm
		},
		path/.style = {
			->,
			thick,
			>=stealth'
		},
		cov/.style = {
			<->,
			thick,
			>=stealth'
		},
	]
	\node[manifest] (y1) {$Y_{1}$};
    \node[manifest] (y2) [right = of y1] {$Y_{2}$};
    \node[manifest] (y3) [right = of y2] {$Y_{3}$};
    \node[manifest] (y4) [right = of y3] {$Y_{4}$};
    \node[mean] (mean) [above = of y1] {$1$};
    \draw[cov] (mean) to[out = 120, in = 60, looseness = 5] node[above] {$0$} (mean);
    \draw[path] (mean) to node {$\alpha_{1}$} (y1);
    \draw[path] (mean) to node {$\alpha_{2}$} (y2);
    \draw[path] (mean) to node {$\alpha_{3}$} (y3);
    \draw[path] (mean) to node {$\alpha_{4}$} (y4);
\end{tikzpicture}

\end{document}
